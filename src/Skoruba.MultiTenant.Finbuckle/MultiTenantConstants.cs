namespace Skoruba.MultiTenant.Configuration
{
    public class MultiTenantConstants
    {
        // TODO: Consider moving to MultiTenantConfiguration but then the configuration must be bound and used during setup and migration
        public const bool IsMultiTenant = true;
    }
}